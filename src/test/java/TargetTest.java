import java.util.stream.Stream;

@ExtendWith(MockitoExtension.class)
public class TargetTest {

    @ParameterizedTest
    @MethodSource("valuesProvider")
    void itFindsCorrectTargets(List<>)

        //    void itMapsMmsMinutes(String locale, List<LinkedTreeMap<String, ?>> input, List<String> output) {
//        when(soc.getMms()).thenReturn(input);
//    assertThat(overageMapper.mapMms(pricePlan), equalTo(output));

//
//    private static Stream<Arguments> mmsMinutesProvider() {

    }
